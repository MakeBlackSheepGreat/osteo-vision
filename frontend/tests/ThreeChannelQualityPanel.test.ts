import { mount } from "@vue/test-utils"; import { describe, expect, it } from "vitest"; import ThreeChannelQualityPanel from "../src/components/ThreeChannelQualityPanel.vue";
describe("ThreeChannelQualityPanel",()=>{it("shows metrics and heatmap",()=>{const wrapper=mount(ThreeChannelQualityPanel,{props:{quality:{overall:{status:"pass",model_input_channels:["white_light","fluorescence"]},synchronization:{status:"pass",white_fluorescence_delta_ms:12.5,tolerance_ms:100},geometry:{status:"pass",pixel_comparison_allowed:true},overlay_comparison:{available:true,status:"pass",mae_rgb:.12,rmse_rgb:.15,ssim_luma:.82,edge_disagreement:.07,difference_heatmap_path:"difference.png"}},previewUrl:(p:string)=>`/preview/${p}`}});expect(wrapper.text()).toContain("白光/荧光已成对");expect(wrapper.text()).toContain("12.5 ms");expect(wrapper.text()).toContain("0.1200");expect(wrapper.text()).toContain("7.00%");expect(wrapper.find("img").attributes("src")).toContain("difference.png");expect(wrapper.text()).toContain("模型输入始终限定")});it("degrades safely",()=>{const wrapper=mount(ThreeChannelQualityPanel,{props:{quality:{overall:{status:"review_required",model_input_channels:["white_light","fluorescence"]},synchronization:{status:"review_required"},geometry:{status:"pass",pixel_comparison_allowed:true},overlay_comparison:{available:false,status:"unavailable",reason:"device_or_software_overlay_missing"}}}});expect(wrapper.text()).toContain("缺少时间戳");expect(wrapper.text()).toContain("设备叠加图为可选证据");expect(wrapper.text()).toContain("安全降级已启用");expect(wrapper.find("img").exists()).toBe(false)})});
