# 当前事实基线与统一口径

更新日期：2026-07-26
适用代码基线：Git `7fd2ed88bb6849b173c68d70d8c3891275e2a2ea`；工程版本 `0.3.0-rc.2`
核对范围：当前工作区代码、受控配置、本地数据与展示资产、设备资料、模型与质量门。
文档属性：当前事实基线。历史报告仅保留其日期化证据属性，不覆盖本口径。

## 1. 结论摘要

- 平台是本地部署的 Vue 3 + FastAPI + PyTorch 研发验证版平台，主流程覆盖文件准入、病例、JPEG/MP4 分析、医生复核、证据导出与三维参考。
- 官方设备资料确认 4K `3840x2160`、USB3.0 存储、JPEG 图片和 MP4 视频。企业 SDK、驱动和实时设备接口未列入本仓库交付边界。
- 生产运行配置可通过运行预检，主线为 `keyframe_residual_attention_unet_s20260715_20260715`，checkpoint 与 sidecar 已核验，启发式与 prompt 回退在生产模式关闭。
- 当前目标域真实术中 ICG 颌骨骨髓炎记录为 `0`，目标域训练准入记录为 `0`。公开、近域、异域、合成及伪标注资源均只能支持工程验证、数据管线和候选模型开发。
- 默认展示病例为 `OFDVDNET_001` 公开离体鸡腿荧光手术代理；前端静态展示图采用 D083 公开人体 ICG 骨移植视频关键帧。两者均保留非目标域边界。
- 本轮质量检查通过 Python 测试、后端测试、Ruff、mypy、前端类型检查、前端构建、前端测试、独立三维运行时检查、当前文档审计及运行预检。
- 发现一个需收口的配置事实：`configs/inference/osteo_vision.yml` 含 `dual_channel_image`，任务包允许的输入类型列表未含该值，因此通用配置校验返回失败；生产配置校验与运行预检均通过。

## 2. 证据优先级与统一表述

| 优先级 | 事实来源 | 可用于表述的内容 |
| --- | --- | --- |
| P0 | 设备提供方技术资料与输入规范 | 设备文件输入边界、已确认参数 |
| P1 | 当前代码、严格运行配置、测试与运行预检 | 当前可运行功能、启用模型、配置开关、质量门结论 |
| P2 | 本地 manifest、校验记录、模型卡与受控展示资产 | 数据来源、许可、域别、校验、工程指标与展示素材 |
| P3 | 日期化报告、规划稿与历史归档 | 对应日期的设计依据、历史结果、待验证计划 |

统一使用以下边界：

- 平台输出用于研发验证、工程证据和医生复核辅助，不构成临床诊断、切除范围结论或真实术中导航结论。
- ICG 反映灌注、血管通透性与组织活性相关信号；不得将其解释为颌骨骨髓炎特异性真值。
- DICOM Secondary Capture、ZIP 证据包和远程协作属于平台扩展能力；平台核心能力为造影剂设计验证、多模态融合处理和 AI 辅助显微判读。
- 公开代理数据、文献图、合成输入、数字仿体和伪标注指标均须带上来源域、用途边界和医生复核状态。

## 3. 官方输入与交付边界

### 3.1 官方资料已确认事项

官方技术文档的影像系统条目确认：

- 4K 影像摄录分辨率为 `3840x2160`。
- 影像经 USB3.0 存储。
- 图片记录格式为 JPEG，视频记录格式为 MP4。
- 显微镜总放大倍率为 `1.3x-17x`，工作距离为 `200-630 mm`。
- ICG 资料描述约 `750-810 nm` 激发与约 `830 nm` 发射，并将其用于术中血流和灌注相关可视化。

对应证据：设备提供方的受控技术资料与 `research/reports/planning/device_input_spec_extracted_text.md` 摘录。原始资料保留在本地受控位置，不进入版本库。

### 3.2 平台软件边界

| 范围 | 当前口径 |
| --- | --- |
| 平台主输入 | 4K JPEG、MP4 文件上传与分析 |
| JPEG 工作流 | 白光与荧光成对输入、配准、伪彩、融合、ROI 定量与质控 |
| MP4 工作流 | 原始播放、关键帧分析、连续帧串行分析及最近结果同步展示 |
| 医院扩展输入 | JPG 与 AVI；AVI 通过受控转码进入 MP4 通道并保留源文件映射和 SHA256 |
| 设备解耦 | 倍率、工作距离、标定、坐标变换和位姿可由 manifest 或人工元数据录入 |
| 仓库外边界 | 设备驱动、厂商 SDK、采集卡适配、实时硬件数据流和患者级真实导航验证 |

## 4. 当前可运行软件事实

### 4.1 架构与入口

| 模块 | 当前实现 |
| --- | --- |
| 前端 | `frontend/`，Vue 3 + TypeScript 桌面工作站；顶部主流程为数据准入、病例档案、病例工作台、三维导航、医生复核、报告导出 |
| 后端 | `backend/osteo_vision_api/`，FastAPI 路由、病例持久化、分析、复核、导出、三维任务与实时帧服务 |
| 分析核心 | `osteo_vision_core/`，图像处理、模型适配器、数据契约、指标、导航与报告核心 |
| 三维运行时 | `frontend/three-d-runtime/` 独立构建、测试与部署，通过版本化契约与主平台交换数据 |
| 启动入口 | 根目录 `start_platform.cmd` 调用 `scripts/start_platform.ps1`；默认端口为 API `8001`、前端 `5174`、三维运行时 `5175` |

`boots.md` 当前台账记录：生产 API 与服务层 85 个 Python 文件、29,748 行；共享分析核心 116 个 Python 文件、23,478 行；后端测试 55 个 Python 文件、15,250 行。该统计随工作区变化而变化，仅作为本次核对快照。

### 4.2 已完成闭环

- 医院数据批次准入包含机构授权、脱敏、用途、文件签名、可解码性、SHA256、重复项、通道关系和隔离原因码；`admitted` 才能写入病例。
- 病例工作台支持 JPEG 双通道分析、单路 MP4、独立白光/荧光双路 MP4、OFDVDnet 合成三视图与浏览器单路视频流。
- 融合链执行几何一致性检查、荧光灰度化、背景扣除、平移配准、归一化、伪彩、alpha 融合、ROI 量化、三通道质控与证据写出。
- 视频模型输出契约包括 `bone_gate_mask`、`fluorescence_signal_mask`、`risk_mask` 与 `uncertain_mask`。未取得医生 ROI 或提示辅助复核时，`bone_gate_mask` 保持待复核或不可用。
- 医生复核页支持像素掩膜、画笔、橡皮擦、多边形、撤销/重做、版本历史、草稿、提交复核、身份与训练准入状态。
- 导出链覆盖 JSON、CSV、Markdown、DICOM Secondary Capture 与 ZIP 证据包，并保留来源、配置、哈希与复核状态。
- 三维工作台支持 CBCT/STL 导入、对象树、工程表面检查、L1 静态配准和 L2 离线位姿回放。主展示状态固定为 L0 未配准参考，直至真实标定、误差与医生复核证据齐备。

### 4.3 三项固定目标状态

| 能力 | 已实现的软件闭环 | 当前安全状态 |
| --- | --- | --- |
| 患者条件分割 | 临床变量契约、API、持久化、界面、报告与 KiTS23 代理候选 | 运行时回退影像基础结果，空间差异关闭 |
| 骨活性分层 | 低活性候选、过渡复核区、高活性参考、无法判断区与连续评分字段 | D074 代理模型 `engineering_utility_ready=false`，空间输出关闭 |
| 三维配准与离线 AR | manifest 元数据、L1 静态配准、L2 离线回放、误差与失效记录 | `navigation_ready=false`，维持 L0 未配准参考 |

## 5. 当前模型与运行配置

### 5.1 生产运行配置

权威运行文件为 `configs/inference/osteo_vision_strict.yml`：

| 项目 | 当前值 |
| --- | --- |
| 运行档位 | `strict_runtime` |
| fixture 模型 | 关闭 |
| 缺 checkpoint 回退 | 关闭 |
| 启发式关键帧回退 | 关闭 |
| prompt 回退 | 关闭 |
| 必需工具 | `ffmpeg`、`ffprobe` |
| 必需主线模型 | `keyframe_residual_attention_unet_s20260715_20260715` |
| 主线阈值 | `0.4` |
| 4K 分块 | `512 px` tile、`64 px` overlap、批大小 `4` |
| 直播快速路径 | 批大小 `8`、AMP、JPEG overlay、关闭不确定性 TTA |

本轮生产运行预检通过。配置 SHA256 为 `ecea4d2694ab77d8256756ab9af9aa37ec828249a0eaab5f4536774fe6f26e80`；主线 checkpoint SHA256 为 `826e90c2ee3efd45d0d0d979e85a2a3e2dcd60d853d8497f6328e46a406e0d39`；checkpoint sidecar 存在且 `runtime_allowed=true`。

### 5.2 模型证据与可声称范围

| 模型或能力 | 当前角色 | 已记录工程指标 | 表述边界 |
| --- | --- | --- | --- |
| 残差注意力 U-Net keyframe 分割器 | 严格主线 JPEG/MP4 关键帧荧光信号分割 | D046/OFDVDnet 代理锁定测试 Dice `0.917681`、IoU `0.848335`、召回率 `0.9099`、参数量 `403,785` | 公开离体荧光代理，不代表目标域临床性能 |
| 多尺度深度可分离 U-Net | 同协议候选 | Dice `0.8978 ± 0.0113`、4K 模型 P95 `4.25 ms`、峰值显存 `20.14 MB` | 保持候选状态 |
| D025 ConvNeXt 3D 分割器 | CBCT lesion ROI 代理 | 阈值 `0.20`；历史报告记录 Dice `0.6567`、IoU `0.5553` | 3D 病灶代理，不可外推为显微视频指标 |
| 患者条件候选 | 研发验证候选 | 条件 Dice `0.243974`，影像基础 Dice `0.244188`，安全门失败 | 强制影像基础回退 |
| 骨活性多任务候选 | 研发验证候选 | macro Dice `0.733064`；工程门未通过 | 空间结果关闭 |

### 5.3 性能统一口径

| 路径 | 当前证据 | 可用表述 |
| --- | --- | --- |
| 4K tiled 全证据 | 模型 P95 `724.432 ms`、服务端端到端 P95 `5776.683 ms`、峰值显存 `723.579 MB` | 完整 mask、概率图、叠加、候选区和 manifest 的工程生成路径 |
| live_fast 连续帧 | 模型 P95 `36.377 ms`、服务端端到端 P95 `176.457 ms`、峰值显存 `380.134 MB` | 串行受控快速叠加路径，保留延迟元数据 |
| 任务 2 配准与融合计算 | 4K 120 帧工程序列合计 P95 `82.026 ms`；配准 `59.022 ms`，GPU 归一化、伪彩和融合 `21.891 ms` | 项目内部工程门，计时不含解码、证据编码、磁盘、网络与 AI 推理 |

上述性能仅对应本机、版本化工程输入与指定协议。官方正式技术文档未给出任务 2 的毫秒阈值；`<100 ms` 只作为项目内部工程验收描述使用。

## 6. 数据资产与演示资产

### 6.1 当前数据快照

当前活动状态文档记录 15 份来源 manifest、47/47 条记录通过结构检查、138/138 个本地文件通过存在性和 SHA256 检查、核验体积约 5.51 GB。完整分层注册表的日期化快照记录 504 条记录、质量门通过 504 条、目标域记录 0、训练准入记录 0、可进入训练准入检查记录 393 条。

两套统计的范围不同：前者是当前 15 份来源 manifest 的受控核验集合；后者是 D046 分层注册表及其派生/候选记录集合。对外报告必须附带统计范围与日期，避免将两者合并成单一总数。

| 数据层 | 代表资源 | 当前用途 | 禁止外推 |
| --- | --- | --- | --- |
| 公开荧光代理 | D046 OFDVDnet/FGS，离体鸡腿模拟荧光手术 | 关键帧训练、阈值扫描、MP4 演示、伪彩与时序工程 | 人体、颌骨骨髓炎或 ICG 临床性能 |
| 公开近域视频 | PMC 骨髓炎公开视频 | 场景参考、流程与复核验证 | ICG 信号或像素真值 |
| 公开人体 ICG | D083 血管化骨移植物视频 | 前端展示、融合、时序与证据流程 | 颌骨骨髓炎、病理边界或医生骨面真值 |
| 颌面/CBCT 解剖 | D024、D025、D036 等 | 解剖先验、代理分割、三维表面与数字仿体验证 | 真实术中导航或颌骨骨髓炎临床性能 |
| 近域论文图与公开临床骨荧光图 | D047、D048 | 弱标签复核种子与来源追溯 | 直接训练或目标域金标准 |
| 临床变量与空间组学代理 | KiTS23、D051、ORN 相关资源 | 字段、缺失值、条件模块与数据治理联调 | 患者条件目标域分割性能 |

### 6.2 标准演示病例与静态资产

- 平台启动默认写入并载入 `OFDVDNET_001`。来源为 D046 OFDVDnet，场景为模拟离体鸡腿荧光手术，manifest 标记 `fluorescence_proxy`、`target_domain_flag=false`、`training_eligible=false`。
- 默认三维参考关联 D024 公开下颌表面参考；该资产用于 L0/L1/L2 工程展示，不能用于患者级导航结论。
- `frontend/public/showcase/` 现有 D083 原图、叠加图、风险图和不确定性图共 4 个受控前端静态资产。来源为 PMC9478374、DOI `10.3389/FSURG.2022.962450`、CC BY 4.0。
- 静态展示资产均已在 Git 跟踪；其完整 SHA256 应以 `artifacts/data_review/d083_icg_video_evidence_20260718/` 的证据报告为准。

## 7. 本轮核验结果

| 检查 | 结果 | 说明 |
| --- | --- | --- |
| 设备与输入技术资料 | 已读取 | 原始资料受控保存；技术文档文本抽取可复核 |
| 活动文档审计 | 通过 | 23 份活动文档，0 error、0 warning |
| 项目 readiness | 通过 | 开发运行 0 error、2 warning；严格运行 0 error、0 warning |
| 严格运行预检 | 通过 | 模型、sidecar、ffmpeg、ffprobe 均可用 |
| Python 测试收集 | 通过 | 当前测试集合可被 Python 3.11.15 收集 |
| `tests/unit tests/smoke backend/tests/unit` | 通过 | 全部通过；存在 FastAPI 与 Torch 上游弃用警告 |
| `backend/tests` | 通过 | 全部通过；存在 FastAPI 上游弃用警告 |
| Ruff | 通过 | `osteo_vision_core backend app tests scripts tools` |
| mypy | 通过 | `osteo_vision_core backend` |
| Vue 类型检查与构建 | 通过 | 212 个模块完成构建 |
| Vue Vitest | 通过 | 57 文件、241 测试 |
| 独立三维运行时 | 通过 | 类型检查、21 测试与构建均通过；构建产生大于 500 kB 的 chunk 提示 |

## 8. 发现项与处置口径

### F1：通用开发配置的任务契约校验失败

`configs/inference/osteo_vision.yml` 和生产配置中的骨活性候选均使用 `dual_channel_image`，而 `configs/tasks/osteo_vision.yml` 的 `input_contract.input_types` 未包含该输入类型。对生产配置执行通用 `validate_config_file` 时得到：`Model 1: Invalid input type: dual_channel_image`。

- 影响范围：配置静态校验与开发档位一致性；不影响本轮已通过的严格运行预检。
- 当前事实：严格配置可通过 runtime readiness，其模型清单中的主线仅使用 `2d_image`；通用开发配置的全量模型注册仍存在契约偏差。
- 当前事实：生产主线“可预检并可运行”；通用开发配置“待修正输入类型契约后再作为完整有效配置使用”。
- 建议处置：在任务输入契约中受控加入 `dual_channel_image`，或将相关候选模型输入类型转换为既有契约语义；同时补充配置校验回归测试。

### F2：质量门记录中的历史测试总数不可与当前输出混用

部分日期化报告记录 `604/288`、`812/352` 等历史测试总数；当前直接运行的命令输出只显示进度条且未打印总数。今后报告应记录“命令、执行日期、通过状态、警告摘要”，除非输出保存了可审计的精确总数。

### F3：独立三维运行时构建包体提示

三维运行时构建通过，但 Vite 报告主 chunk 约 `720.19 kB`，超过默认 `500 kB` 提示线。该项未阻断功能；后续可评估按 Three.js 场景、加载器或调试能力分包。

### F4：DOCX 临时文件与用户既有未提交材料

当前工作区有用户已有的提交报告 README 修改、2026-07-26 精简报告 DOCX/Markdown、Office 临时锁文件、`.trae/` 与 `tmp/` 未跟踪项。本报告未修改或清理这些用户工作内容。

## 9. 后续报告与展示写法

后续 README、可行性报告、UI 文案和证据导出统一采用以下写法：

- “平台支持官方 4K JPEG/MP4 文件输入；企业设备接口由设备侧负责。”
- “主线模型在 D046/OFDVDnet 公开离体荧光代理上的工程指标为 Dice 0.917681、IoU 0.848335。”
- “当前真实术中 ICG 颌骨骨髓炎目标域记录和训练准入记录均为 0。”
- “D083 为公开人体 ICG 骨移植视频工程代理；D024/D036 为公开三维解剖或数字仿体工程参考。”
- “任务 2 的 82.026 ms 是指定 4K 工程序列上的项目内部配准与融合计算 P95，未包含文件 I/O、网络传输和任务 3 AI 推理。”
- “三维主展示为 L0 未配准参考；L1/L2 仅反映数字/离线工程验证。”
- “候选区、风险图、不确定性图、患者条件结果和骨活性结果均需医生复核，并保留安全回退。”

## 10. 权威引用入口

- 设备技术参数摘录：`research/reports/planning/device_input_spec_extracted_text.md`
- 当前目标母稿：`research/reports/planning/osteo_vision_platform_target_zh.md`
- 当前平台状态：`docs/project_summary.md`
- 当前生产配置：`configs/inference/osteo_vision_strict.yml`
- 任务输出契约：`configs/tasks/osteo_vision.yml`
- 当前标准演示资产说明：`frontend/public/showcase/README.md`
- 数据层级登记：`research/datasets/public-candidates/d046_fluorescence_osteomyelitis_videos/derived/layered_registry_20260711/layered_dataset_registry.csv`
- 主线选型报告：`research/reports/modeling/keyframe_model_selection_summary_20260715_zh.md`
- 稳定性收尾证据：`research/reports/release/runtime_stability_closeout_20260721_zh.md`
